[phases.setup]
nixPkgs = ["python313"]

[phases.install]
cmds = [
  "python -m venv .venv",
  ".venv/bin/pip install -r requirements.txt"
]

[start]
cmd = ".venv/bin/gunicorn athena.wsgi --bind 0.0.0.0:$PORT --log-file -"
